# 2026-07-07 工作日志

## 第一轮：简化设置面板 + 增加原图最大尺寸设置（commit f759818）

### 需求
1. 移除超分设置中的：超分算法、降噪级别、输出格式
2. 增加「原图最大尺寸」设置（宽度/高度，缺省 1024px）
3. 检测原图太大的逻辑使用设置值而非硬编码 1024

### 改动

#### HTML（index.html）
- **删除**：超分算法选择 (.model-selector)、降噪级别 (denoiseLevel + range-labels)、输出格式 (imageFormatGroup + videoFormatGroup)
- **新增**：原图最大尺寸区块
  ```html
  <div class="max-size-inputs">
      <div class="max-size-field">
          <label>宽度</label>
          <input type="number" id="maxWidthInput" value="1024" min="0">
          <span>px</span>
      </div>
      <div class="max-size-field">
          <label>高度</label>
          <input type="number" id="maxHeightInput" value="1024" min="0">
          <span>px</span>
      </div>
  </div>
  ```

#### CSS（style.css）
- **新增**：`.max-size-inputs`（flex, gap:16px）、`.max-size-field`（flex, align-items:center）、`.max-size-field .setting-input`（80px, text-align:center, focus-ring）

#### JS（app.js）
- **AppState.settings**：删除 `model`/`denoise`，新增 `maxWidth: 1024`/`maxHeight: 1024`
- **initElements**：删除 `denoiseLevel`/`denoiseValue`/`imageFormatGroup`/`videoFormatGroup`，新增 `maxWidthInput`/`maxHeightInput`
- **initSettings**：删除模型选择绑定、降噪绑定、格式按钮绑定，新增 maxWidth/maxHeight input 绑定
- **删除 `updateFormatDisplay()` 函数**及两处调用（switchMode + showPreview）
- **showPreview 图片信息**：`img.width > maxW && img.height > maxH`
- **startProcessing**：日志消息不再显示 model/denoise，改为显示 maxWidth/maxHeight；最大尺寸检查改用 AppState.settings.maxWidth/maxHeight
- **缓存版本号** v25→v26

### 未改动
- 图片对比滑块逻辑
- 视频对比滑块逻辑
- server.py（H.264 重编码等）
